// Sidebar navigation for the SDK docs section.
import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  index: 'Libraries',
  typescript: 'TypeScript Reference',
  'direct-rpc': 'Direct RPC',
}

export default meta
